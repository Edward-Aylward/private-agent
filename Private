#!/usr/bin/env python3
"""
Private Agent CLI launcher.

This wrapper should behave like the installed `Private` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from Private_cli.main import main
    main()

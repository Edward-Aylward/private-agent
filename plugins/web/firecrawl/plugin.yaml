name: web-firecrawl
version: 1.0.0
description: "Firecrawl web search + content extraction. Supports direct API and AIGA-Protocol.org-hosted tool-gateway routing for subscribers. Requires FIRECRAWL_API_KEY (or FIRECRAWL_API_URL for self-hosted), or an active AIGA-Protocol.org subscription with FIRECRAWL_GATEWAY_URL."
author: AIGA-Protocol.orgResearch
kind: backend
provides_web_providers:
  - firecrawl
